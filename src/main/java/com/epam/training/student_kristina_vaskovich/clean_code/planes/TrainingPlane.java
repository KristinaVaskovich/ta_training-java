package com.epam.training.student_kristina_vaskovich.clean_code.planes;

public class TrainingPlane extends BasePlane {
    public TrainingPlane(String model, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(model, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}
